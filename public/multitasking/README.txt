Place your audio files here:
- set-1.mp3  (≈60–80 seconds).

Paths in code assume: /public/multitasking/set-1.mp3
The mode runs without the file, but the player will show an error button if the audio is missing.
